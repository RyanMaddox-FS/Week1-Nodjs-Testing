const add = (a, b) => a + b;

const subtract = (c, d) => c - d;

const multiply = (e, f) => e * f;

const divide = (g, h) => g / h;

const squareRoot = i => Math.sqrt(i);

const maxNumber = (j, k, l) => Math.max(j, k, l);

module.exports={add, subtract, multiply, divide, squareRoot, maxNumber};